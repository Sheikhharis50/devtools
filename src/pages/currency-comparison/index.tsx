import Rates from "@/components/CurrencyComparison";
import Layout from "@/components/Layout";

const CurrencyComparisonPage = () => {
  return (
    <Layout>
      <Rates />
    </Layout>
  );
};

export default CurrencyComparisonPage;
